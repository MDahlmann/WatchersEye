# Docker compose used for demo setup

name: watchers-demo

services:

# PostgreSQL database
  db:
    image: postgres:18-alpine
    container_name: demo-poe-postgres
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - pgdata:/var/lib/postgresql/data


# Backend API
  api:
    labels:
      - "com.centurylinklabs.watchtower.scope=demo"
    image: ghcr.io/mdahlmann/poetracker-api:latest
    container_name: demo-poe-api
    restart: always
    environment:
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - ASPNETCORE_ENVIRONMENT=Production
      - OriginApiUrl=${DEMO_ORIGIN_API_URL}
      - ActiveLeague=${DEMO_ACTIVE_LEAGUE}
      - SyncInterval=30000
      - DiscordWebhook=${DEMO_DISCORD_WEBHOOK}
      - UseMockData=true
    depends_on:
      - db
    networks:
      - default
      - watchers-net


# Frontend project
  client:
    labels:
      - "com.centurylinklabs.watchtower.scope=demo"
    image: ghcr.io/mdahlmann/poetracker-client:latest
    container_name: demo-poe-client
    restart: always
    depends_on:
      - api
    networks:
      - default
      - watchers-net


# Fetches new images from GitHub Container Registry
# and replaces running containers with them.
  watchtower:
    image: containrrr/watchtower
    container_name: demo-watchtower
    restart: always
    environment:
      - DOCKER_API_VERSION=1.44
      - WATCHTOWER_SCOPE=demo
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ~/.docker/config.json:/config.json:ro
    command: --interval 60 --cleanup

volumes:
  pgdata:
    name: demo_db_data

networks:
  watchers-net:
    external: true